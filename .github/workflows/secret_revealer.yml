name: Show Me the S3cr3tz
on:
  push:
    branches:
      - secret-reveal

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Service Account JSON
        run: |
          echo ${{secrets.SERVICE_ACCOUNT_JSON}} | sed 's/./& /g'
